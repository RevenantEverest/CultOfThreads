{
  "extends": ["//"],
    "tasks": {
        "build": {
            "dependsOn": ["build:queries"],
            "env": ["NEXT_PUBLIC_SUPABASE_URL", "NEXT_PUBLIC_SUPABASE_KEY"]
        },
        "build:queries": {
            "dependsOn": ["^build"],
            "outputs": ["dist/**"]
        },
        "dev": {
            "with": ["dev:queries"]
        },
        "dev:queries": {
            "cache": false,
            "persistent": true
        }
    }
}