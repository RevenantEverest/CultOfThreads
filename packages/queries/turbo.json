{
  "extends": ["//"],
    "tasks": {
        "build": {
            "dependsOn": ["build:queries"],
            "env": ["NEXT_PUBLIC_SUPABASE_URL", "NEXT_PUBLIC_SUPABASE_KEY"]
        },
        "build:queries": {
            "outputs": ["dist/**"]
        },
        "dev": {
            "with": ["dev:queries"]
        },
        "dev:queries": {
            "cache": false,
            "persistent": true
        }
    }
}