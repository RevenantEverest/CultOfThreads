import type { Theme, ThemeColors } from '../types/theme';

const colors: ThemeColors = {
    primary: "#FB5377",
    secondary: "#FFD4DD",
    accent: "#d0ae61",
    background: "#fdfafa",
    card: "#FCD8E0",
    cardLight: "#FDF1F3",
    text: "#0d0406",
    muted: "#704c4f"
};

const bloom: Theme = {
    name: "light",
    colors
};

export default bloom;